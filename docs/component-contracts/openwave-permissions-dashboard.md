# OpenWave Permissions Dashboard

The consent dashboard shows active, pending, expired, and revoked permissions. Each consent exposes provider, purpose, scopes, linked accounts, status, expiry, last access, and revoke/renew capabilities.

The UI must make access understandable and revocation discoverable.

