# Account Workspace Segment Contract V3

`SignalPrismWorkspaceSegmentedControl` keeps account details progressive.

## Segments

- نظرة عامة
- الحركات
- الطلبات
- المستندات

Only the selected segment renders below the control. Do not expose every account feature at once.

Segments must be RTL-native, compact, accessible, and visually calmer than primary actions.
