# Account Overview Section Contract

The overview segment shows only the most useful account state:

- compact balance breakdown
- package/limits summary
- linked services summary
- latest account activity preview when useful

Do not show documents or requests in Overview. Those belong behind their own segments.
