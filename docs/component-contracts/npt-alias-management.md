# NPT Alias Management

Alias management lists receiving aliases, linked accounts, status, default marker, copy action, and add/relink actions.

Aliases render as identifiers and remain backend-driven. Copy actions should avoid logging values and may use sensitive clipboard metadata depending on bank policy.

