# Prism Gradient Token Contract

All gradients must come from `SignalPrismGradientTokens`.

Components may select the appropriate role but must not invent local color stops. If a bank overrides gradient endpoints, the override enters through `SignalPrismProfile` or bank palette configuration.

Gradients must be protected with scrims or text-safe zones when financial text sits on top.
