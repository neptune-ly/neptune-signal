# Prism Experience Resolver Contract

`SignalPrismExperienceResolver` merges bank policy and user preference into a safe resolved experience.

The resolver clamps unsupported values:
- unsupported moods fall back to Neptune
- unsupported card modes fall back to carousel
- unsupported nav styles fall back to Floating Dock

Future work should add persisted user preferences and accessibility/system constraints.

